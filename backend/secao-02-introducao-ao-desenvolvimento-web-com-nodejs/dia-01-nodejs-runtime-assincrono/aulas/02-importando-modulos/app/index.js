const {
  celsiusToFahrenheit,
  fahrenheitToCelsius,
} = require("../mymodules/temp"); // Módulo local

const path = require("path"); // Módulo interno

console.log(celsiusToFahrenheit(100));
console.log(fahrenheitToCelsius(212));
