# Exercício 02 - Manipulação e Busca

## 🧠 Objetivo do exercício
O objetivo deste exercício foi praticar o uso de comandos do terminal voltados para **busca, listagem e manipulação de arquivos** no ambiente `Unix & Shell`.  

---

## 🧩 O que foi feito
Durante as atividades, criei um novo diretório de trabalho e utilizei o comando `curl` para baixar um arquivo contendo o nome de todos os países do mundo.  
Em seguida, explorei formas diferentes de visualizar o conteúdo do arquivo diretamente pelo terminal, utilizando comandos para:
- Exibir o conteúdo completo de arquivos de texto;
- Visualizar o conteúdo página por página;
- Buscar palavras específicas dentro dos arquivos;
- Realizar buscas ignorando diferenças entre letras maiúsculas e minúsculas.

Na segunda parte do exercício, trabalhei com um novo arquivo de texto, aplicando comandos para:
- Filtrar linhas que **não** contêm uma palavra específica;
- Contar a quantidade de **palavras** e **linhas** em um arquivo.

Por fim, pratiquei a criação de arquivos vazios com diferentes extensões e o uso de expressões para:
- Listar arquivos com padrões específicos no nome (como `.txt` e `.tbt`);
- Consultar o **manual de ajuda** de comandos diretamente pelo terminal.

---

## 💡 O que aprendi
Com essas práticas, aprendi a:
- Utilizar o comando `curl` para baixar arquivos diretamente da internet;
- Usar comandos como `cat`, `less`, `grep` e `wc` para explorar e analisar arquivos de texto;
- Aplicar filtros e buscas dentro de arquivos de forma eficiente;
- Trabalhar com padrões e extensões de arquivos utilizando o `ls` com curingas (`*`);
- Consultar a documentação de comandos via terminal usando `man`.

---

## 🧪 Projeto ou prática final
Como prática final, organizei e manipulei diferentes arquivos dentro do diretório `unix_tests_search`, consolidando o uso dos principais comandos de busca e listagem do terminal.

---

## 🗂️ Tecnologias utilizadas
- `Unix`
- `Shell`
- `curl`
- `grep`
- `wc`
- `ls`

---

## 💭 Reflexão pessoal
Esse exercício me ajudou a compreender como o terminal pode ser uma ferramenta poderosa para buscar e manipular dados rapidamente. A prática com comandos como `grep` e `wc` mostrou o quanto é possível automatizar tarefas simples sem depender de interfaces gráficas.