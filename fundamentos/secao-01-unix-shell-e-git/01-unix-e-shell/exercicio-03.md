# Exercício 03 - Comandos de input e output

## 🧠 Objetivo do exercício
O objetivo deste exercício foi aprofundar o uso de **comandos de input e output no terminal**, praticando como criar, editar, combinar e analisar arquivos de texto diretamente pela linha de comando.

---

## 🧩 O que foi feito
Durante as atividades, criei um diretório de trabalho e utilizei o terminal para gerar e manipular arquivos de texto.  
Iniciei criando o arquivo `skills2.txt`, adicionando manualmente uma lista de habilidades relacionadas à área de tecnologia. Em seguida, aprendi a:
- Inserir e ordenar conteúdo dentro de arquivos;
- Contar a quantidade de linhas existentes em um arquivo;
- Criar um novo arquivo (`top_skills.txt`) contendo apenas as três primeiras habilidades em ordem alfabética.

Depois, produzi outro arquivo chamado `phrases2.txt`, onde adicionei frases personalizadas.  
Com ele, pratiquei:
- Contar linhas que **contêm** ou **não contêm** determinadas combinações de letras;
- Acrescentar novos conteúdos ao final do arquivo.

Por fim, combinei o conteúdo de dois arquivos (`phrases2.txt` e `countries.txt`) em um novo arquivo chamado `bunch_of_things.txt`, e então utilizei comandos para **ordenar alfabeticamente** o conteúdo final.

---

## 💡 O que aprendi
Com essa prática, aprendi a:
- Criar e editar arquivos de texto diretamente pelo terminal;
- Usar comandos como `cat`, `sort`, `wc`, `grep` e `echo` para manipular dados;
- Combinar múltiplos arquivos em um único documento;
- Contar linhas e filtrar conteúdo com base em padrões de texto;
- Utilizar operadores de redirecionamento (`>`, `>>`) para gerar e modificar arquivos.

---

## 🧪 Projeto ou prática final
Como prática final, construí um fluxo completo de manipulação de arquivos, desde a criação até a ordenação e combinação de dados, aplicando diversos comandos de entrada e saída no terminal.

---

## 🗂️ Tecnologias utilizadas
- `Unix`
- `Shell`
- `grep`
- `sort`
- `wc`
- `cat`
- `echo`

---

## 💭 Reflexão pessoal
Esse exercício foi essencial para consolidar meu domínio sobre os comandos de entrada e saída no terminal. Entendi como é possível criar e manipular arquivos de forma rápida, sem precisar de editores gráficos, tornando o fluxo de trabalho muito mais eficiente.